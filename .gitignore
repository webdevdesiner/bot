node_modules/
.env
.env.*
!.env.example
!dashboard/.env.example
*.db
.DS_Store
Thumbs.db

# WhatsApp Web.js — sessão local (não versionar)
.wwebjs_auth/
wwebjs-data/
.wwebjs_cache/
sessions.json
bot-orion-web.zip

# Ícones gerados localmente para o instalador (icon:build)
build/

# Chrome copiado para o instalador (gerar com npm run vendor:puppeteer)
vendor/

# Electron — artefatos de build (instalador .exe etc.)
release/

# Dashboard — build Vite (regenerar com npm run dashboard:build)
dashboard/dist/

# Logs locais
logs/
