# Copie para dashboard/.env e ajuste a URL do ngrok (sem barra no final).
# Também existe public/api-link.json (copiado para dist/) para o painel na Hostinger.
# Depois: npm run dashboard:build
#
# VITE_API_BASE=https://acetabuliform-lina-unsinewed.ngrok-free.dev
